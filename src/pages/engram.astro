---
import BaseLayout from '../layouts/BaseLayout.astro';

const engramLd = {
  "@context": "https://schema.org",
  "@type": "SoftwareApplication",
  "name": "Engram",
  "description": "Open-source memory for AI agents. Temporal knowledge graph with fact extraction, conflict detection, hybrid retrieval, and consolidation. Works with any framework through MCP.",
  "applicationCategory": "DeveloperApplication",
  "operatingSystem": "Linux, macOS, Windows",
  "url": "https://jamjet.dev/engram/",
  "offers": { "@type": "Offer", "price": "0", "priceCurrency": "USD" },
};
---

<BaseLayout
  title="Engram — Memory for AI Agents | JamJet"
  description="Open-source memory for AI agents. Temporal knowledge graph with hybrid retrieval, conflict detection, and MCP-native tools. Works with Claude, Cursor, LangChain, CrewAI, and any MCP client."
  canonical="https://jamjet.dev/engram/"
>
<Fragment slot="head">
  <script type="application/ld+json" set:html={JSON.stringify(engramLd)}></script>
</Fragment>

<!-- ── Hero ── -->
<section class="engram-hero">
  <div class="engram-hero-inner">
    <span class="section-label">Part of JamJet</span>
    <h1>Memory for<br/>AI agents.</h1>
    <p class="engram-sub">
      Your AI agent forgets everything between sessions. Engram gives it persistent,
      intelligent memory &mdash; a temporal knowledge graph that extracts facts, detects
      conflicts, and consolidates over time. Open source. Works with any framework through MCP.
    </p>
    <div class="engram-actions">
      <a href="#quickstart" class="btn-primary">Start in 60 seconds</a>
      <a href="https://github.com/jamjet-labs/jamjet/tree/main/runtime/engram-server" class="btn-secondary" target="_blank" rel="noopener">View source</a>
    </div>
  </div>
</section>

<!-- ── Problem ── -->
<section style="background: var(--bg-alt);">
  <div class="container">
    <div class="problem-grid">
      <div class="problem-item">
        <strong>No memory</strong>
        <p>Every conversation starts from zero. Your agent re-asks the same questions, forgets preferences, loses project context.</p>
      </div>
      <div class="problem-item">
        <strong>Vector stores are not memory</strong>
        <p>Embedding raw messages is not understanding. There is no fact extraction, no conflict resolution, no temporal awareness.</p>
      </div>
      <div class="problem-item">
        <strong>Too much infrastructure</strong>
        <p>Many memory tools need an external vector or graph database before you can store a single fact. Engram starts with a single SQLite file and moves to Postgres only when you need it.</p>
      </div>
      <div class="problem-item">
        <strong>Framework lock-in</strong>
        <p>Most memory solutions are tied to one framework. Switch from LangChain to CrewAI and your memory layer breaks.</p>
      </div>
    </div>
  </div>
</section>

<!-- ── What Engram Does ── -->
<section>
  <div class="container">
    <span class="section-label">How Engram works</span>
    <h2>Not a vector store.<br/>A knowledge graph that learns.</h2>
    <div class="features-grid">
      <div class="feature">
        <strong>Fact extraction</strong>
        <p>Raw conversation messages go in. Structured facts come out. Entities, relationships, preferences, events &mdash; all extracted automatically via LLM.</p>
      </div>
      <div class="feature">
        <strong>Conflict detection</strong>
        <p>When new information contradicts existing facts, Engram detects the conflict and supersedes the old fact. "I moved to Austin" invalidates "I live in Seattle."</p>
      </div>
      <div class="feature">
        <strong>Hybrid retrieval</strong>
        <p>Vector search + FTS5 keyword search + graph spreading activation. Six retrieval signals combined: semantic similarity, temporal proximity, node importance, calibrated confidence, keyword match, and graph connections.</p>
      </div>
      <div class="feature">
        <strong>Consolidation</strong>
        <p>Background engine that decays stale facts, promotes important ones, deduplicates, summarizes entity histories, and builds preference rollups. Memory stays clean over time.</p>
      </div>
      <div class="feature">
        <strong>Temporal grounding</strong>
        <p>Facts carry timestamps. Queries understand "last week" and "recently." Context output is date-annotated so your agent knows when things happened.</p>
      </div>
      <div class="feature">
        <strong>Provider-agnostic</strong>
        <p>One binary speaks Ollama (local, free), OpenAI-compatible endpoints, Anthropic Claude, Google Gemini, or a shell-out command. Set one env var.</p>
      </div>
    </div>
  </div>
</section>

<!-- ── Memory in Action ── -->
<section>
  <div class="container">
    <span class="section-label">Memory in action</span>
    <h2>Watch facts accumulate, conflicts resolve.</h2>
    <p style="color: var(--text-secondary); max-width: 640px; margin: 16px 0 0; line-height: 1.7;">
      Two short scenarios. The first shows what your agent remembers across sessions.
      The second shows how Engram supersedes a stale fact when reality changes.
    </p>

    <div class="demo-pair">
      <!-- Demo 1: Before / After session memory -->
      <div class="demo-card">
        <div class="demo-card-head">
          <span class="demo-tag">Scenario 1</span>
          <strong>Before / after memory</strong>
        </div>
        <div class="demo-split">
          <div class="demo-side">
            <div class="demo-side-label">Session 1</div>
            <pre class="demo-pre"><code><span class="t-d"># User says:</span>
"I prefer concise answers and
my project is JamJet Cloud."

<span class="t-d"># Engram extracts:</span>
<span class="t-kw">fact</span> user.style    = <span class="t-s">"concise"</span>
<span class="t-kw">fact</span> user.project  = <span class="t-s">"JamJet Cloud"</span></code></pre>
          </div>
          <div class="demo-arrow" aria-hidden="true">
            <svg viewBox="0 0 60 24" width="60" height="24"><path d="M2 12 H56 M50 6 L56 12 L50 18" stroke="currentColor" stroke-width="1.5" fill="none" stroke-linecap="round" stroke-linejoin="round"/></svg>
            <span>seven sessions later</span>
          </div>
          <div class="demo-side">
            <div class="demo-side-label">Session 8</div>
            <pre class="demo-pre"><code><span class="t-d"># User says:</span>
"Draft the release note for
the dashboard."

<span class="t-d"># Agent recalls:</span>
project = <span class="t-s">"JamJet Cloud"</span>
style   = <span class="t-s">"concise"</span>
recent  = <span class="t-s">"policy violations panel"</span></code></pre>
          </div>
        </div>
      </div>

      <!-- Demo 2: Conflict resolution graph -->
      <div class="demo-card">
        <div class="demo-card-head">
          <span class="demo-tag">Scenario 2</span>
          <strong>Conflict resolution</strong>
        </div>
        <div class="demo-conflict">
          <svg viewBox="0 0 480 220" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Knowledge graph: an old fact 'Sunil lives in Pune' is superseded by a newer message 'I moved to Amsterdam'">
            <!-- old fact -->
            <g class="cf-node cf-old">
              <rect x="20" y="40" width="160" height="48" rx="8"/>
              <text x="100" y="60" text-anchor="middle" class="cf-title">Sunil — lives in</text>
              <text x="100" y="78" text-anchor="middle" class="cf-value">Pune</text>
            </g>
            <text x="100" y="104" text-anchor="middle" class="cf-meta">stored Jan 2025</text>

            <!-- new statement -->
            <g class="cf-node cf-new">
              <rect x="300" y="40" width="160" height="48" rx="8"/>
              <text x="380" y="60" text-anchor="middle" class="cf-title">incoming</text>
              <text x="380" y="78" text-anchor="middle" class="cf-value">"I moved to Amsterdam"</text>
            </g>
            <text x="380" y="104" text-anchor="middle" class="cf-meta">today</text>

            <!-- arrow -->
            <g class="cf-arrow">
              <path d="M180 64 H300" stroke-linecap="round" stroke-linejoin="round"/>
              <path d="M292 58 L300 64 L292 70" stroke-linecap="round" stroke-linejoin="round" fill="none"/>
            </g>

            <!-- decision badge -->
            <g class="cf-decision">
              <rect x="140" y="140" width="200" height="56" rx="10"/>
              <text x="240" y="160" text-anchor="middle" class="cf-decision-title">supersede</text>
              <text x="240" y="180" text-anchor="middle" class="cf-decision-sub">newer explicit statement &middot; same entity</text>
            </g>
            <path class="cf-decision-line" d="M240 110 V140" stroke-linecap="round" stroke-dasharray="3 3"/>
          </svg>
          <p class="demo-foot">
            Old fact is kept in history (replayable, auditable) but no longer surfaces in retrieval.
            Reason and timestamp are recorded.
          </p>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ── Works With ── -->
<section style="background: var(--bg-alt);">
  <div class="container" style="text-align: center;">
    <span class="section-label">Works with everything via MCP</span>
    <h2>One memory layer. Any framework.</h2>
    <p style="color: var(--text-secondary); max-width: 560px; margin: 16px auto 40px;">
      Engram exposes 11 MCP tools. Any client that speaks MCP can use it &mdash; no adapter code, no integration library, no lock-in.
    </p>
    <div class="compat-grid">
      <div class="compat-item">Claude Desktop</div>
      <div class="compat-item">Cursor</div>
      <div class="compat-item">VS Code</div>
      <div class="compat-item">Windsurf</div>
      <div class="compat-item">LangChain</div>
      <div class="compat-item">CrewAI</div>
      <div class="compat-item">Spring AI</div>
      <div class="compat-item">Custom agents</div>
    </div>
    <p style="color: var(--text-muted); font-size: 0.85rem; margin-top: 24px;">
      Also available as: Rust library (embed in your app) &middot; REST API &middot; Python client &middot; Java client &middot; Spring Boot starter
    </p>
  </div>
</section>

<!-- ── Quickstart ── -->
<section id="quickstart">
  <div class="container">
    <span class="section-label">Get started</span>
    <h2>Running in 60 seconds.</h2>
    <div class="qs-grid">
      <div class="qs-option">
        <h3>Docker (recommended)</h3>
        <pre class="qs-code"><code>docker run --rm -i \
  -v engram-data:/data \
  ghcr.io/jamjet-labs/engram-server:0.5.0</code></pre>
        <p>Uses local Ollama by default. Zero config.</p>
      </div>
      <div class="qs-option">
        <h3>Cargo install</h3>
        <pre class="qs-code"><code>cargo install jamjet-engram-server
engram serve --db memory.db</code></pre>
        <p>Native binary. ~3MB. Starts instantly.</p>
      </div>
      <div class="qs-option">
        <h3>MCP config (Claude Desktop)</h3>
        <pre class="qs-code"><code set:html={`{
  "mcpServers": {
    "memory": {
      "command": "engram",
      "args": ["serve", "--db", "memory.db"]
    }
  }
}`}></code></pre>
        <p>Add to your MCP config and restart. Done.</p>
      </div>
    </div>
    <div class="qs-tools">
      <span class="section-label" style="margin-bottom: 12px;">11 MCP tools</span>
      <div class="tools-list">
        <code>memory_add</code>
        <code>memory_recall</code>
        <code>memory_context</code>
        <code>memory_search</code>
        <code>memory_forget</code>
        <code>memory_stats</code>
        <code>memory_consolidate</code>
        <code>messages_save</code>
        <code>messages_get</code>
        <code>messages_list</code>
        <code>messages_delete</code>
      </div>
    </div>
  </div>
</section>

<!-- ── Comparison ── -->
<section style="background: var(--bg-alt);">
  <div class="container">
    <span class="section-label">How Engram compares</span>
    <h2>Honest comparison.</h2>
    <div class="compare-table-wrap">
      <table class="compare-table">
        <thead>
          <tr>
            <th>Capability</th>
            <th>Engram</th>
            <th>Mem0</th>
            <th>Zep</th>
            <th>Letta</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>Fact extraction</td>
            <td class="yes">Yes (LLM-based)</td>
            <td class="yes">Yes</td>
            <td class="partial">Basic</td>
            <td class="no">No</td>
          </tr>
          <tr>
            <td>Conflict detection</td>
            <td class="yes">Yes (auto-supersede)</td>
            <td class="partial">Partial</td>
            <td class="no">No</td>
            <td class="no">No</td>
          </tr>
          <tr>
            <td>Knowledge graph</td>
            <td class="yes">Yes (entities + relationships)</td>
            <td class="partial">v2 (new)</td>
            <td class="yes">Yes</td>
            <td class="no">No</td>
          </tr>
          <tr>
            <td>Hybrid retrieval</td>
            <td class="yes">Vector + FTS5 + graph (6 signals)</td>
            <td class="partial">Vector + graph</td>
            <td class="yes">Hybrid</td>
            <td class="partial">Vector only</td>
          </tr>
          <tr>
            <td>Consolidation engine</td>
            <td class="yes">5 ops (decay, promote, dedup, summarize, reflect)</td>
            <td class="no">No</td>
            <td class="no">No</td>
            <td class="no">No</td>
          </tr>
          <tr>
            <td>MCP server</td>
            <td class="yes">Yes (11 tools)</td>
            <td class="no">No</td>
            <td class="no">No</td>
            <td class="no">No</td>
          </tr>
          <tr>
            <td>Zero-infra quickstart</td>
            <td class="yes">SQLite (single file)</td>
            <td class="no">Needs Qdrant</td>
            <td class="no">Needs Neo4j + Docker</td>
            <td class="no">Needs PostgreSQL</td>
          </tr>
          <tr>
            <td>LLM provider choice</td>
            <td class="yes">Ollama, OpenAI, Anthropic, Google, shell-out</td>
            <td class="partial">OpenAI default</td>
            <td class="partial">OpenAI default</td>
            <td class="partial">OpenAI default</td>
          </tr>
          <tr>
            <td>Message store</td>
            <td class="yes">Built-in (save/get/list/delete)</td>
            <td class="no">No</td>
            <td class="yes">Yes</td>
            <td class="yes">Yes</td>
          </tr>
          <tr>
            <td>Spring Boot starter</td>
            <td class="yes">Maven Central</td>
            <td class="no">No</td>
            <td class="no">No</td>
            <td class="no">No</td>
          </tr>
          <tr>
            <td>Maturity</td>
            <td class="partial">New (v0.5.0, small community)</td>
            <td class="yes">Established (large community)</td>
            <td class="yes">Established</td>
            <td class="yes">Established (ex-MemGPT)</td>
          </tr>
        </tbody>
      </table>
    </div>
    <p style="color: var(--text-muted); font-size: 0.85rem; text-align: center; margin-top: 20px;">
      All frameworks evolve. Check their docs for the latest. Engram&apos;s advantage is MCP-native distribution + zero-infra + consolidation.
      Its disadvantage is a smaller community and fewer production deployments.
    </p>
  </div>
</section>

<!-- ── Distribution ── -->
<section>
  <div class="container" style="text-align: center;">
    <span class="section-label">Available everywhere</span>
    <h2>5 registries. One install.</h2>
    <div class="registries-grid">
      <a href="https://crates.io/crates/jamjet-engram" target="_blank" rel="noopener" class="registry-item">
        <strong>crates.io</strong>
        <span>jamjet-engram</span>
      </a>
      <a href="https://crates.io/crates/jamjet-engram-server" target="_blank" rel="noopener" class="registry-item">
        <strong>crates.io</strong>
        <span>jamjet-engram-server</span>
      </a>
      <a href="https://pypi.org/project/jamjet/" target="_blank" rel="noopener" class="registry-item">
        <strong>PyPI</strong>
        <span>jamjet (engram module)</span>
      </a>
      <a href="https://central.sonatype.com/artifact/dev.jamjet/jamjet-sdk" target="_blank" rel="noopener" class="registry-item">
        <strong>Maven Central</strong>
        <span>dev.jamjet:jamjet-sdk</span>
      </a>
      <a href="https://ghcr.io/jamjet-labs/engram-server" target="_blank" rel="noopener" class="registry-item">
        <strong>GHCR</strong>
        <span>Docker image</span>
      </a>
      <a href="https://registry.modelcontextprotocol.io/servers/io.github.jamjet-labs/engram-server" target="_blank" rel="noopener" class="registry-item">
        <strong>MCP Registry</strong>
        <span>Official listing</span>
      </a>
    </div>
  </div>
</section>

<!-- ── Hosted Engram ── -->
<section>
  <div class="container">
    <div class="hosted-engram">
      <div class="hosted-engram-text">
        <span class="section-label">Hosted Engram</span>
        <h2>Want memory without<br/>running a server?</h2>
        <p>
          Hosted Engram is a managed surface inside JamJet Cloud. Same MCP API, same retrieval
          quality &mdash; no Postgres, no Docker, no provisioning. Memory spaces per agent, retention
          controls per project, audit trail for every read and write.
        </p>
        <div style="display:flex;gap:12px;flex-wrap:wrap;margin-top:24px;">
          <a href="/cloud" class="btn-primary">View JamJet Cloud</a>
          <a href="mailto:hello@jamjet.dev?subject=Hosted%20Engram%20waitlist" class="btn-secondary">Join waitlist</a>
        </div>
      </div>
      <div class="hosted-engram-grid">
        <div class="hosted-card"><strong>Managed Postgres</strong><span>Auto-provisioned, encrypted, backed up</span></div>
        <div class="hosted-card"><strong>API keys &amp; scopes</strong><span>Per-agent, per-project, per-environment</span></div>
        <div class="hosted-card"><strong>Team memory spaces</strong><span>Shared facts across an agent fleet</span></div>
        <div class="hosted-card"><strong>Retention controls</strong><span>TTL, archival, hard delete on request</span></div>
        <div class="hosted-card"><strong>Audit logs</strong><span>Every read and write, exportable</span></div>
        <div class="hosted-card"><strong>Export &amp; delete</strong><span>Move out anytime &mdash; no lock-in</span></div>
      </div>
    </div>
  </div>
</section>

<!-- ── Upsell to JamJet ── -->
<section style="background: var(--bg-alt);">
  <div class="container" style="text-align: center; max-width: 600px;">
    <h2>Need more than memory?</h2>
    <p style="color: var(--text-secondary); margin: 16px 0 32px; line-height: 1.7;">
      Engram is part of JamJet &mdash; the open-source safety layer for AI agents.
      When your agents need policy checks, audit trails, human approval, crash recovery,
      and cost limits, JamJet has it built in.
    </p>
    <div style="display: flex; justify-content: center; gap: 12px; flex-wrap: wrap;">
      <a href="#quickstart" class="btn-primary">Add memory to Claude Desktop</a>
      <a href="/" class="btn-secondary">Learn about JamJet</a>
    </div>
  </div>
</section>

<style>
  .engram-hero {
    min-height: 70vh;
    display: flex; align-items: center; justify-content: center;
    padding: 80px 24px;
    text-align: center;
  }
  .engram-hero-inner { max-width: 680px; }
  .engram-sub {
    font-size: clamp(1rem, 2vw, 1.1rem);
    color: var(--text-secondary);
    margin: 24px 0 40px;
    line-height: 1.8;
  }
  .engram-actions {
    display: flex; justify-content: center; gap: 12px; flex-wrap: wrap;
  }

  .problem-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 32px 64px;
  }
  .problem-item {
    padding: 24px 0;
    border-top: 1px solid var(--border);
  }
  .problem-item strong {
    display: block; font-size: 0.95rem; margin-bottom: 8px; color: var(--text);
  }
  .problem-item p {
    color: var(--text-secondary); font-size: 0.88rem; line-height: 1.7; margin: 0;
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 32px 64px;
    margin-top: 48px;
  }
  .feature {
    padding: 24px 0;
    border-top: 1px solid var(--border);
  }
  .feature strong {
    display: block; font-size: 0.95rem; margin-bottom: 8px; color: var(--text);
  }
  .feature p {
    color: var(--text-secondary); font-size: 0.88rem; line-height: 1.7; margin: 0;
  }

  .compat-grid {
    display: flex;
    justify-content: center;
    flex-wrap: wrap;
    gap: 12px;
  }
  .compat-item {
    padding: 10px 20px;
    border: 1px solid var(--border);
    border-radius: 8px;
    font-size: 0.88rem;
    font-family: var(--font-sans);
    font-weight: 500;
    color: var(--text-secondary);
  }

  .qs-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 32px;
    margin-top: 40px;
  }
  .qs-option h3 {
    font-size: 0.92rem; margin-bottom: 12px;
  }
  .qs-code {
    background: var(--bg-code);
    color: #eae8e1;
    padding: 16px;
    border-radius: 8px;
    font-size: 0.82rem;
    line-height: 1.6;
    overflow-x: auto;
    margin-bottom: 12px;
  }
  .qs-code code {
    font-family: var(--font-mono);
  }
  .qs-option p {
    color: var(--text-muted); font-size: 0.85rem;
  }

  .qs-tools {
    margin-top: 48px;
    text-align: center;
  }
  .tools-list {
    display: flex;
    justify-content: center;
    flex-wrap: wrap;
    gap: 8px;
  }
  .tools-list code {
    padding: 6px 14px;
    background: var(--bg-alt);
    border: 1px solid var(--border);
    border-radius: 6px;
    font-size: 0.82rem;
    color: var(--text-secondary);
  }

  .compare-table-wrap {
    overflow-x: auto;
    margin-top: 32px;
  }
  .compare-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 0.85rem;
    min-width: 600px;
  }
  .compare-table th {
    font-size: 0.72rem;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    color: var(--text-muted);
    font-weight: 600;
    padding: 12px 16px;
    border-bottom: 2px solid var(--border-strong);
    text-align: left;
    font-family: var(--font-sans);
  }
  .compare-table td {
    padding: 10px 16px;
    border-bottom: 1px solid var(--border);
    color: var(--text-secondary);
    line-height: 1.5;
  }
  .compare-table td.yes { color: var(--text); font-weight: 500; }
  .compare-table td.partial { color: var(--text-muted); }
  .compare-table td.no { color: var(--text-muted); opacity: 0.6; }

  .registries-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 16px;
    max-width: 700px;
    margin: 32px auto 0;
  }
  .registry-item {
    display: flex;
    flex-direction: column;
    padding: 16px;
    border: 1px solid var(--border);
    border-radius: 8px;
    text-decoration: none;
    transition: border-color 0.15s;
  }
  .registry-item:hover { border-color: var(--accent); }
  .registry-item strong {
    font-size: 0.82rem;
    font-family: var(--font-sans);
    color: var(--text);
    margin-bottom: 4px;
  }
  .registry-item span {
    font-size: 0.78rem;
    color: var(--text-muted);
    font-family: var(--font-mono);
  }

  /* ── Memory in action demos ── */
  .demo-pair {
    display: grid;
    grid-template-columns: 1fr;
    gap: 24px;
    margin-top: 40px;
  }
  .demo-card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 10px;
    padding: 24px 28px 28px;
  }
  .demo-card-head {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-bottom: 18px;
  }
  .demo-card-head strong {
    font-size: 1rem;
    color: var(--text);
  }
  .demo-tag {
    padding: 3px 10px;
    border-radius: 999px;
    background: var(--bg-alt);
    color: var(--text-muted);
    font-size: 0.7rem;
    font-family: var(--font-sans);
    text-transform: uppercase;
    letter-spacing: 0.06em;
    font-weight: 600;
  }
  .demo-split {
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    gap: 16px;
    align-items: center;
  }
  .demo-side-label {
    font-size: 0.72rem;
    font-family: var(--font-sans);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--text-muted);
    font-weight: 600;
    margin-bottom: 8px;
  }
  .demo-pre {
    background: var(--bg-code, #0e1116);
    color: #eae8e1;
    padding: 16px 18px;
    border-radius: 8px;
    font-size: 0.78rem;
    line-height: 1.7;
    font-family: var(--font-mono);
    margin: 0;
    overflow-x: auto;
  }
  .demo-pre .t-d { color: #888; }
  .demo-pre .t-kw { color: #c8a2ff; }
  .demo-pre .t-s { color: #a3d977; }
  .demo-arrow {
    display: flex;
    flex-direction: column;
    align-items: center;
    color: var(--text-muted);
  }
  .demo-arrow span {
    font-size: 0.7rem;
    font-family: var(--font-sans);
    text-transform: uppercase;
    letter-spacing: 0.06em;
    margin-top: 6px;
    color: var(--text-muted);
  }
  .demo-conflict {
    text-align: center;
  }
  .demo-conflict svg {
    width: 100%;
    height: auto;
    max-width: 480px;
    color: var(--text);
  }
  .cf-node rect {
    stroke-width: 1.5;
    fill: var(--surface);
    stroke: var(--border-strong, var(--border));
  }
  .cf-old rect {
    stroke-dasharray: 4 3;
    opacity: 0.7;
  }
  .cf-new rect { fill: var(--bg-alt); stroke: var(--text); }
  .cf-title {
    font-size: 11px;
    fill: var(--text-muted);
    font-family: var(--font-sans);
    text-transform: uppercase;
    letter-spacing: 0.06em;
  }
  .cf-value {
    font-size: 14px;
    fill: var(--text);
    font-family: var(--font-mono);
    font-weight: 500;
  }
  .cf-meta {
    font-size: 10px;
    fill: var(--text-muted);
    font-family: var(--font-sans);
  }
  .cf-arrow path {
    stroke: var(--text-muted);
    stroke-width: 1.5;
    fill: none;
  }
  .cf-decision rect {
    fill: var(--text);
    stroke: var(--text);
  }
  .cf-decision-title {
    font-size: 13px;
    fill: var(--bg);
    font-family: var(--font-sans);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.08em;
  }
  .cf-decision-sub {
    font-size: 10px;
    fill: var(--bg);
    opacity: 0.7;
    font-family: var(--font-sans);
  }
  .cf-decision-line {
    stroke: var(--text-muted);
    fill: none;
  }
  .demo-foot {
    color: var(--text-muted);
    font-size: 0.85rem;
    line-height: 1.6;
    margin-top: 16px;
    max-width: 480px;
    margin-left: auto;
    margin-right: auto;
  }

  /* ── Hosted Engram ── */
  .hosted-engram {
    display: grid;
    grid-template-columns: 0.85fr 1fr;
    gap: 56px;
    align-items: center;
  }
  .hosted-engram-text h2 { margin: 8px 0 16px; }
  .hosted-engram-text p {
    color: var(--text-secondary);
    line-height: 1.75;
  }
  .hosted-engram-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 12px;
  }
  .hosted-card {
    padding: 14px 16px;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 8px;
  }
  .hosted-card strong {
    display: block;
    font-size: 0.92rem;
    color: var(--text);
    margin-bottom: 4px;
  }
  .hosted-card span {
    font-size: 0.82rem;
    color: var(--text-muted);
    line-height: 1.5;
  }

  @media (max-width: 768px) {
    .problem-grid { grid-template-columns: 1fr; }
    .features-grid { grid-template-columns: 1fr; }
    .qs-grid { grid-template-columns: 1fr; }
    .registries-grid { grid-template-columns: 1fr; }
    .demo-split { grid-template-columns: 1fr; }
    .demo-arrow { transform: rotate(90deg); margin: 8px 0; }
    .hosted-engram { grid-template-columns: 1fr; gap: 32px; }
    .hosted-engram-grid { grid-template-columns: 1fr; }
  }
</style>
</BaseLayout>
