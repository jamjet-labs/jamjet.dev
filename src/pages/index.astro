---
import BaseLayout from '../layouts/BaseLayout.astro';
import HeroAnimation from '../components/HeroAnimation.astro';
import TerminalReplay from '../components/TerminalReplay.astro';

const terminalLines = [
  '$ jamjet run research-pipeline.yaml',
  '▸ Starting execution exec_7f3a...',
  '▸ [Plan]       ✓ completed  420ms',
  '▸ [Research]   ✓ completed  1.2s',
  '▸ [Analyze]    ✗ worker crashed',
  '▸ Lease expired · reclaiming...',
  '▸ [Analyze]    ✓ resumed    890ms',
  '▸ [Review]     ✓ completed  650ms',
  '▸ [Synthesize] ✓ completed  1.1s',
  '▸ Execution complete · 5/5 nodes · 0 events lost',
];

const homeLd = {
  "@context": "https://schema.org",
  "@type": "SoftwareApplication",
  "name": "JamJet",
  "description": "The durable runtime for AI agents. Checkpoint replay, native MCP + A2A, built-in eval, full execution traces. Write Python, run with Rust reliability.",
  "applicationCategory": "DeveloperApplication",
  "operatingSystem": "Linux, macOS, Windows",
  "url": "https://jamjet.dev",
  "downloadUrl": "https://pypi.org/project/jamjet/",
  "softwareVersion": "0.3.1",
  "license": "https://www.apache.org/licenses/LICENSE-2.0",
  "offers": { "@type": "Offer", "price": "0", "priceCurrency": "USD" },
};
---

<BaseLayout
  title="JamJet — Build AI Agents That Don't Lose Their Work"
  description="JamJet is the durable runtime for production-grade AI agents. Checkpoint replay, native MCP + A2A, built-in eval, full execution traces. Write Python, run with Rust reliability."
  canonical="https://jamjet.dev/"
>
<Fragment slot="head">
  <script type="application/ld+json" set:html={JSON.stringify(homeLd)}></script>
</Fragment>

<!-- ── Hero ── -->
<section class="hero">
  <div class="hero-inner">
    <h1>Build AI agents that<br/>don't lose their work.</h1>
    <p class="hero-sub">
      JamJet is the durable runtime for AI agents &mdash; with checkpoint replay,
      full execution traces, and runtime-enforced reliability.<br/>
      Write Python. Run with Rust reliability.
    </p>
    <div class="hero-actions">
      <a href="#proof" class="btn-primary">See the crash recovery demo &darr;</a>
      <a href="https://docs.jamjet.dev/en/docs/quickstart" class="btn-secondary">Get started in 60 seconds</a>
    </div>
    <div class="install-cmd">
      <span class="prompt">$</span>
      <span class="cmd">pip install jamjet</span>
      <button class="copy-btn" id="copy-install">copy</button>
    </div>
  </div>
</section>

<!-- ── New: Engram Announcement Strip ── -->
<section class="announcement-strip">
  <div class="container">
    <a href="/blog/engram-agent-memory/" class="announcement">
      <span class="announcement-badge">NEW</span>
      <span class="announcement-text">
        <strong>Engram</strong> &mdash; the first Java-native memory layer for AI agents is live.
        Fact extraction, temporal knowledge graph, hybrid retrieval, consolidation engine. One SQLite file.
      </span>
      <span class="announcement-arrow">&rarr;</span>
    </a>
  </div>
</section>

<!-- ── Proof: Crash Recovery Demo ── -->
<section id="proof">
  <div class="container">
    <span class="section-label">See it survive</span>
    <h2>Your agents crash. Ours recover.</h2>
    <p class="section-intro">
      Every step is checkpointed as it happens. When a worker dies mid-run,
      the scheduler reclaims the lease and resumes exactly where it left off.
    </p>
    <div class="proof-anim">
      <HeroAnimation />
    </div>
    <div class="proof-grid">
      <div>
        <TerminalReplay lines={terminalLines} title="jamjet · crash recovery" />
      </div>
      <div class="proof-callouts">
        <div class="proof-step">
          <span class="proof-icon">1</span>
          <div>
            <strong>Worker crashes mid-execution</strong>
            <p>The Analyze step fails unexpectedly.</p>
          </div>
        </div>
        <div class="proof-step">
          <span class="proof-icon">2</span>
          <div>
            <strong>Lease reclaimed automatically</strong>
            <p>The scheduler detects the failure and reclaims work.</p>
          </div>
        </div>
        <div class="proof-step">
          <span class="proof-icon">3</span>
          <div>
            <strong>Resumes from checkpoint</strong>
            <p>No rerun of completed steps. Picks up exactly where it stopped.</p>
          </div>
        </div>
        <div class="proof-step">
          <span class="proof-icon">4</span>
          <div>
            <strong>Zero events lost</strong>
            <p>All 5 nodes complete. Full execution integrity preserved.</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ── Why This Matters ── -->
<section class="why-matters">
  <div class="container">
    <div class="why-matters-grid">
      <div class="why-matters-item">
        <strong>No lost work after failure</strong>
        <p>Completed steps stay completed. A crash on step 6 doesn't rerun steps 1–5.</p>
      </div>
      <div class="why-matters-item">
        <strong>No repeated side effects</strong>
        <p>External calls aren't re-executed. Emails aren't re-sent. Charges aren't re-applied.</p>
      </div>
      <div class="why-matters-item">
        <strong>No rerun cost for completed steps</strong>
        <p>Tokens already spent stay spent. Resume costs only what remains.</p>
      </div>
      <div class="why-matters-item">
        <strong>Exact replay for debugging and audit</strong>
        <p>Replay the full execution from checkpoints. See exactly what happened, when.</p>
      </div>
    </div>
  </div>
</section>

<!-- ── Outcomes ── -->
<section style="background: var(--bg-alt);">
  <div class="container">
    <span class="section-label">What you get</span>
    <h2 class="section-title">Production outcomes,<br/>not feature lists</h2>
    <div class="outcomes">
      <div class="outcome">
        <svg class="outcome-icon" viewBox="0 0 32 32" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
          <path d="M6 16h6l3-8 4 16 3-8h4" />
          <circle cx="27" cy="16" r="2" fill="currentColor" stroke="none" />
        </svg>
        <h3>Recover from crashes without rerunning everything</h3>
        <p>Completed steps are never repeated. JamJet resumes from the exact point of failure instead of forcing a full rerun.</p>
      </div>
      <div class="outcome">
        <svg class="outcome-icon" viewBox="0 0 32 32" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
          <rect x="8" y="6" width="16" height="20" rx="2" />
          <line x1="13" y1="16" x2="19" y2="16" />
          <line x1="16" y1="13" x2="16" y2="19" />
          <circle cx="16" cy="16" r="6" stroke-dasharray="2 2" />
        </svg>
        <h3>Pause safely for human approval</h3>
        <p>Approval gates suspend durably and survive restarts. Long-running workflows stay reliable even when humans are in the loop.</p>
      </div>
      <div class="outcome">
        <svg class="outcome-icon" viewBox="0 0 32 32" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
          <polyline points="6,24 12,14 18,18 26,8" />
          <polyline points="20,8 26,8 26,14" />
          <line x1="6" y1="28" x2="26" y2="28" stroke-dasharray="2 2" />
        </svg>
        <h3>Replay any execution to debug exactly what happened</h3>
        <p>Replay a run from checkpoints, inspect traces, and see per-node cost and latency. No guessing from logs.</p>
      </div>
      <div class="outcome">
        <svg class="outcome-icon" viewBox="0 0 32 32" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="10" cy="16" r="4" />
          <circle cx="22" cy="16" r="4" />
          <line x1="14" y1="16" x2="18" y2="16" />
          <line x1="6" y1="8" x2="10" y2="12" />
          <line x1="6" y1="24" x2="10" y2="20" />
          <line x1="22" y1="12" x2="26" y2="8" />
          <line x1="22" y1="20" x2="26" y2="24" />
        </svg>
        <h3>Connect tools and external agents with MCP + A2A</h3>
        <p>Use built-in client and server support for both protocols. Connect to tools, delegate to agents, and keep every call inside a durable execution model.</p>
      </div>
      <div class="outcome">
        <svg class="outcome-icon" viewBox="0 0 32 32" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
          <rect x="6" y="6" width="20" height="20" rx="2" />
          <line x1="6" y1="20" x2="26" y2="20" />
          <line x1="12" y1="20" x2="12" y2="26" />
          <rect x="10" y="10" width="12" height="6" rx="1" stroke-dasharray="2 2" />
        </svg>
        <h3>Enforce cost and iteration limits in the runtime</h3>
        <p>Cost caps, token budgets, and iteration limits are enforced by the runtime, not left to application discipline.</p>
      </div>
      <div class="outcome">
        <svg class="outcome-icon" viewBox="0 0 32 32" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
          <path d="M8 22l4-4 4 4 8-8" />
          <circle cx="24" cy="14" r="2" />
          <rect x="6" y="6" width="20" height="20" rx="2" />
        </svg>
        <h3>Evaluate agents like software, not vibes</h3>
        <p>Run evals as workflow nodes with judge-based, assertion-based, latency, and cost scoring. Fail CI on regressions.</p>
      </div>
      <div class="outcome">
        <svg class="outcome-icon" viewBox="0 0 32 32" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="16" cy="6" r="4" />
          <line x1="16" y1="10" x2="16" y2="16" />
          <line x1="16" y1="16" x2="8" y2="24" />
          <line x1="16" y1="16" x2="24" y2="24" />
          <line x1="16" y1="16" x2="16" y2="26" />
          <circle cx="8" cy="26" r="2" />
          <circle cx="16" cy="28" r="2" />
          <circle cx="24" cy="26" r="2" />
        </svg>
        <h3>Route to the best agent automatically</h3>
        <p>The Coordinator discovers agents at runtime, scores them on capability, cost, and latency, and routes to the best fit. Optional LLM tiebreaker when scores are close. Every routing decision lands in the event log.</p>
      </div>
      <div class="outcome">
        <svg class="outcome-icon" viewBox="0 0 32 32" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
          <rect x="2" y="4" width="12" height="10" rx="2" />
          <rect x="18" y="4" width="12" height="10" rx="2" />
          <path d="M14 9h4" stroke-dasharray="2 2" />
          <rect x="10" y="20" width="12" height="10" rx="2" />
          <path d="M8 14v3a3 3 0 003 3h0" />
          <path d="M24 14v3a3 3 0 01-3 3h0" />
        </svg>
        <h3>Use agents as tools — three modes</h3>
        <p>Wrap any agent as a callable tool. Sync for quick tasks. Streaming with early termination for long-running work. Conversational for multi-turn refinement — all with budget enforcement.</p>
      </div>
      <div class="outcome">
        <svg class="outcome-icon" viewBox="0 0 32 32" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
          <rect x="8" y="4" width="16" height="24" rx="2" />
          <line x1="12" y1="10" x2="20" y2="10" />
          <line x1="12" y1="14" x2="20" y2="14" />
          <line x1="12" y1="18" x2="16" y2="18" />
          <path d="M18 22l2 2 4-4" />
        </svg>
        <h3>Deploy with enterprise controls when needed</h3>
        <p>Tenant isolation, PII redaction, OAuth delegation, mTLS federation, and retention controls — enforced at the runtime layer.</p>
      </div>
    </div>
  </div>
</section>

<!-- ── Code Example ── -->
<section>
  <div class="container">
    <div class="code-section">
      <div class="code-text">
        <span class="section-label">Authoring</span>
        <h2>Your language.<br/>Production-grade<br/>from day one.</h2>
        <p style="color: var(--text-secondary); margin-top: 16px;">
          Agent strategies, workflow orchestration, cost guardrails, crash recovery. Define agents and workflows in Python or Java &mdash; the Rust runtime handles the rest.
        </p>
      </div>
      <div class="code-block">
        <div class="code-header">
          <div class="code-tabs">
            <button class="code-tab active" data-tab="python">Python</button>
            <button class="code-tab" data-tab="java">Java</button>
          </div>
          <span class="code-tab-label" id="code-tab-label">durable workflow</span>
        </div>
        <pre class="code-body" id="code-python" is:raw><span class="t-kw">from</span> <span class="t-w">jamjet</span> <span class="t-kw">import</span> <span class="t-w">task</span><span class="t-p">,</span> <span class="t-w">workflow</span><span class="t-p">,</span> <span class="t-w">approval</span>

<span class="t-p">@</span><span class="t-b">task</span><span class="t-p">(</span><span class="t-w">model</span><span class="t-p">=</span><span class="t-s">"claude-sonnet-4-6"</span><span class="t-p">,</span> <span class="t-w">max_cost</span><span class="t-p">=</span><span class="t-y">0.50</span><span class="t-p">)</span>
<span class="t-kw">async def</span> <span class="t-b">analyze</span><span class="t-p">(</span><span class="t-w">data</span><span class="t-p">:</span> <span class="t-y">dict</span><span class="t-p">) -&gt;</span> <span class="t-y">Report</span><span class="t-p">:</span>
    <span class="t-s">"""Analyze data — checkpointed, cost-capped."""</span>

<span class="t-p">@</span><span class="t-b">workflow</span>
<span class="t-kw">async def</span> <span class="t-b">pipeline</span><span class="t-p">(</span><span class="t-w">raw</span><span class="t-p">:</span> <span class="t-y">dict</span><span class="t-p">):</span>
    <span class="t-w">report</span> <span class="t-p">=</span> <span class="t-kw">await</span> <span class="t-b">analyze</span><span class="t-p">(</span><span class="t-w">raw</span><span class="t-p">)</span>       <span class="t-d"># crash-safe</span>
    <span class="t-kw">await</span> <span class="t-b">approval</span><span class="t-p">(</span><span class="t-w">report</span><span class="t-p">)</span>            <span class="t-d"># durable pause</span>
    <span class="t-kw">return await</span> <span class="t-b">publish</span><span class="t-p">(</span><span class="t-w">report</span><span class="t-p">)</span>     <span class="t-d"># resumes here</span></pre>
        <pre class="code-body" id="code-java" style="display:none" is:raw><span class="t-y">Agent</span> <span class="t-w">analyst</span> <span class="t-p">=</span> <span class="t-y">Agent</span><span class="t-p">.</span><span class="t-b">builder</span><span class="t-p">()</span>
    <span class="t-p">.</span><span class="t-b">name</span><span class="t-p">(</span><span class="t-s">"analyst"</span><span class="t-p">)</span>
    <span class="t-p">.</span><span class="t-b">model</span><span class="t-p">(</span><span class="t-s">"claude-sonnet-4-6"</span><span class="t-p">)</span>
    <span class="t-p">.</span><span class="t-b">strategy</span><span class="t-p">(</span><span class="t-y">Strategy</span><span class="t-p">.</span><span class="t-w">REFLECTION</span><span class="t-p">)</span>
    <span class="t-p">.</span><span class="t-b">tools</span><span class="t-p">(</span><span class="t-w">searchTool</span><span class="t-p">,</span> <span class="t-w">sqlTool</span><span class="t-p">)</span>
    <span class="t-p">.</span><span class="t-b">maxCost</span><span class="t-p">(</span><span class="t-y">0.50</span><span class="t-p">)</span>                   <span class="t-d">// budget enforced</span>
    <span class="t-p">.</span><span class="t-b">build</span><span class="t-p">();</span>

<span class="t-y">WorkflowGraph</span> <span class="t-w">pipeline</span> <span class="t-p">=</span> <span class="t-y">WorkflowGraph</span><span class="t-p">.</span><span class="t-b">builder</span><span class="t-p">()</span>
    <span class="t-p">.</span><span class="t-b">node</span><span class="t-p">(</span><span class="t-w">analyst</span><span class="t-p">)</span>                    <span class="t-d">// crash-safe</span>
    <span class="t-p">.</span><span class="t-b">node</span><span class="t-p">(</span><span class="t-y">ApprovalGate</span><span class="t-p">.</span><span class="t-b">create</span><span class="t-p">())</span>     <span class="t-d">// durable pause</span>
    <span class="t-p">.</span><span class="t-b">node</span><span class="t-p">(</span><span class="t-w">publisher</span><span class="t-p">)</span>                  <span class="t-d">// resumes here</span>
    <span class="t-p">.</span><span class="t-b">build</span><span class="t-p">();</span></pre>
      </div>
    </div>
  </div>
</section>

<!-- ── Pick Your Path ── -->
<section class="pick-path">
  <div class="container">
    <span class="section-label">Pick your path</span>
    <h2 class="section-title">One question decides your authoring approach.</h2>
    <p class="section-intro" style="max-width:600px;">All three compile to the same IR and run on the same Rust runtime. Pick the one that fits how your agents think.</p>

    <div class="path-flow">
      <div class="path-question">Do your agents need reasoning strategies?</div>
      <div class="path-branches">
        <div class="path-branch path-yes">
          <span class="path-arrow">Yes</span>
          <div class="path-card path-card-primary">
            <h3>Agent + Workflow SDK</h3>
            <p>Multi-agent systems with specialist agents. Each gets a strategy: <code>react</code>, <code>plan-and-execute</code>, <code>critic</code>. Typed Pydantic state. Human-in-the-loop.</p>
            <code class="path-import">from jamjet import Agent, Workflow, tool</code>
          </div>
        </div>
        <div class="path-branch path-no">
          <span class="path-arrow">No</span>
          <div class="path-fork">
            <div class="path-subq">Config-driven?</div>
            <div class="path-sub-branches">
              <div class="path-card">
                <h3>YAML Workflows</h3>
                <p>Tool pipelines, MCP orchestration, routing. Change flows without code deploys.</p>
                <code class="path-import">jamjet run workflow.yaml</code>
              </div>
              <div class="path-card">
                <h3>Python Decorators</h3>
                <p>Custom logic inside nodes. Full Python library access. Class-based workflows.</p>
                <code class="path-import">from jamjet import workflow, node</code>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <p style="text-align:center;color:var(--text-muted);font-size:0.85rem;margin-top:2rem;">All three produce the same IR &rarr; same Rust runtime &rarr; same durability, replay, and audit trails. <a href="https://docs.jamjet.dev/en/docs/concepts#choosing-an-authoring-approach" style="color:var(--accent);">Full guide &rarr;</a></p>

    <div class="capabilities-strip">
      <span class="capabilities-label">Layer on any approach</span>
      <div class="capabilities-grid">
        <a href="https://java-ai-memory.dev" class="cap-item">
          <span class="cap-icon">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><path d="M12 6v6l4 2"/></svg>
          </span>
          <span class="cap-name">Engram Memory</span>
          <span class="cap-desc">Knowledge graphs, semantic search, durable recall</span>
        </a>
        <a href="https://docs.jamjet.dev/en/docs/mcp" class="cap-item">
          <span class="cap-icon">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="2" y="6" width="20" height="12" rx="2"/><path d="M6 12h4m4 0h4"/></svg>
          </span>
          <span class="cap-name">MCP Tools</span>
          <span class="cap-desc">Connect to databases, APIs, file systems via standard protocol</span>
        </a>
        <a href="https://docs.jamjet.dev/en/docs/a2a" class="cap-item">
          <span class="cap-icon">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M16 3l5 5-5 5"/><path d="M21 8H9"/><path d="M8 21l-5-5 5-5"/><path d="M3 16h12"/></svg>
          </span>
          <span class="cap-name">A2A Protocol</span>
          <span class="cap-desc">Cross-framework agent-to-agent communication</span>
        </a>
        <a href="https://docs.jamjet.dev/en/docs/eval" class="cap-item">
          <span class="cap-icon">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M9 11l3 3L22 4"/><path d="M21 12v7a2 2 0 01-2 2H5a2 2 0 01-2-2V5a2 2 0 012-2h11"/></svg>
          </span>
          <span class="cap-name">Eval Harness</span>
          <span class="cap-desc">LLM judge, assertions, cost scoring &mdash; in CI</span>
        </a>
      </div>
    </div>
  </div>
</section>

<!-- ── Migration Strip ── -->
<section style="background: var(--bg-alt);">
  <div class="container">
    <div class="migration-strip">
      <div class="migration-main">
        <span class="section-label">Switching is easy</span>
        <h2>Coming from LangGraph?</h2>
        <div class="migration-points">
          <div class="migration-point">
            <strong>Same mental model</strong>
            <p>Typed state, workflow steps, routing, and graph-like structure — without relearning how to think.</p>
          </div>
          <div class="migration-point">
            <strong>Durability by default</strong>
            <p>No optional checkpoint plumbing. Every step is durable when you run on the JamJet runtime.</p>
          </div>
          <div class="migration-point">
            <strong>Validation at every boundary</strong>
            <p>Typed schemas catch state and interface problems before they spread.</p>
          </div>
          <div class="migration-point">
            <strong>Runtime-enforced limits</strong>
            <p>Budgets and iteration caps live in the scheduler, not in scattered guard code.</p>
          </div>
        </div>
        <div class="migration-actions">
          <a href="https://docs.jamjet.dev/en/docs/migrate/from-langgraph" class="btn-primary">Read the migration guide</a>
          <a href="https://docs.jamjet.dev/en/docs/compare" class="btn-secondary">Compare all frameworks</a>
        </div>
      </div>
      <div class="migration-also">
        <p class="migration-also-label">At a glance</p>
        <div class="migration-compare">
          <div class="compare-row">
            <span class="compare-name">Plain Python</span>
            <span class="compare-desc">Easy start, no durability</span>
          </div>
          <div class="compare-row">
            <span class="compare-name">LangGraph</span>
            <span class="compare-desc">Graph orchestration, optional checkpoints</span>
          </div>
          <div class="compare-row compare-highlight">
            <span class="compare-name">JamJet</span>
            <span class="compare-desc">Python mental model + durability by default</span>
          </div>
        </div>
        <p class="migration-also-label" style="margin-top: 20px;">Also migrating from:</p>
        <a href="https://docs.jamjet.dev/en/docs/migrate/from-crewai">CrewAI &rarr;</a>
        <a href="https://docs.jamjet.dev/en/docs/migrate/from-openai-direct">OpenAI Agents SDK &rarr;</a>
      </div>
    </div>
  </div>
</section>

<!-- ── Three Persona Cards ── -->
<section>
  <div class="container">
    <span class="section-label">One runtime, different reasons to care</span>
    <h2 class="section-title">Built for your role</h2>
    <div class="personas">
      <div class="persona">
        <svg class="persona-icon" viewBox="0 0 36 36" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
          <path d="M8 28V8l10 5 10-5v20" />
          <line x1="18" y1="13" x2="18" y2="28" stroke-dasharray="2 3" />
          <circle cx="18" cy="21" r="3" />
        </svg>
        <span class="persona-label">For Builders</span>
        <h3>Ship agents that survive production</h3>
        <p>Use your Python mental model, but get durable execution, replayable traces, and runtime-enforced safety. Start with <code>@task</code>, scale to full workflows without rewrites.</p>
        <a href="https://docs.jamjet.dev/en/docs/quickstart">Read the quickstart &rarr;</a>
      </div>
      <div class="persona">
        <svg class="persona-icon" viewBox="0 0 36 36" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
          <rect x="6" y="8" width="24" height="16" rx="2" />
          <line x1="6" y1="14" x2="30" y2="14" />
          <line x1="14" y1="14" x2="14" y2="24" />
          <circle cx="10" cy="11" r="1" fill="currentColor" stroke="none" />
          <circle cx="22" cy="19" r="3" stroke-dasharray="2 2" />
          <path d="M14 28h8" />
          <line x1="18" y1="24" x2="18" y2="28" />
        </svg>
        <span class="persona-label">For Platform Teams</span>
        <h3>Standardize agent infrastructure</h3>
        <p>One runtime for reliability, observability, eval, and governance. Tenant isolation, PII redaction, OAuth delegation, and mTLS &mdash; all enforced at the Rust layer.</p>
        <a href="https://docs.jamjet.dev/en/docs/enterprise">Enterprise controls &rarr;</a>
      </div>
      <div class="persona">
        <svg class="persona-icon" viewBox="0 0 36 36" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
          <path d="M10 28c0-6 3-8 8-8s8 2 8 8" />
          <circle cx="18" cy="12" r="5" />
          <line x1="28" y1="10" x2="32" y2="6" />
          <line x1="28" y1="6" x2="32" y2="10" />
          <line x1="30" y1="6" x2="30" y2="10" />
        </svg>
        <span class="persona-label">For Researchers</span>
        <h3>Run reproducible experiments</h3>
        <p>The same runtime properties that make agents reliable in production make experiments reproducible in research. ExperimentGrid, checkpoint replay, publication-ready export.</p>
        <a href="/research">Explore the research toolkit &rarr;</a>
      </div>
    </div>
  </div>
</section>

<!-- ── Research (Reframed) ── -->
<section style="background: var(--bg-alt);">
  <div class="container">
    <span class="section-label">Research</span>
    <h2 class="section-title">Reliable agents for production.<br/>Reproducible agents for research.<br/>Same runtime.</h2>
    <div class="research-grid">
      <div class="research-card">
        <span class="research-card-label">Compare</span>
        <p>Sweep 6 strategies across models and seeds in one command. Parallel execution with durable checkpoints across every condition.</p>
      </div>
      <div class="research-card">
        <span class="research-card-label">Replay</span>
        <p>Replay the exact failed run. Fork with modified inputs for ablations. No need to rebuild infrastructure or re-run completed conditions.</p>
      </div>
      <div class="research-card">
        <span class="research-card-label">Export</span>
        <p>Paper-ready LaTeX tables with mean &plusmn; std, CSV, and JSON. From experiment to results without custom scripts or manual formatting.</p>
      </div>
    </div>
    <div style="text-align:center;margin-top:48px;">
      <a href="/research" class="btn-primary">Explore the research toolkit</a>
    </div>
  </div>
</section>

<!-- ── Agentic Design Patterns ── -->
<section style="background: var(--bg-alt);">
  <div class="container">
    <span class="section-label">Patterns</span>
    <h2 class="section-title">Six orchestration patterns.<br/>One runtime.</h2>
    <p style="color: var(--text-secondary); text-align:center; max-width:640px; margin:0 auto 48px;">
      Every major multi-agent pattern is a first-class primitive — not a workaround.
      Pick the pattern that fits your problem. JamJet handles the durability, observability, and governance.
    </p>
    <div class="outcomes">
      <div class="outcome">
        <div style="font-size:2rem;margin-bottom:12px;">&#x1f9e9;</div>
        <h3>Single Agent</h3>
        <p><code style="background:var(--surface-code);padding:2px 6px;border-radius:3px;font-size:0.85em">@task</code> decorator. 3 lines of Python. Best for simple prototypes and single-purpose tasks.</p>
      </div>
      <div class="outcome">
        <div style="font-size:2rem;margin-bottom:12px;">&#x27a1;&#xfe0f;</div>
        <h3>Sequential Pipeline</h3>
        <p>Chain nodes in a <code style="background:var(--surface-code);padding:2px 6px;border-radius:3px;font-size:0.85em">WorkflowGraph</code>. Each step depends on the previous. Best for ETL, document processing, multi-stage analysis.</p>
      </div>
      <div class="outcome">
        <div style="font-size:2rem;margin-bottom:12px;">&#x1f500;</div>
        <h3>Parallel Fan-Out</h3>
        <p><code style="background:var(--surface-code);padding:2px 6px;border-radius:3px;font-size:0.85em">ParallelNode</code> runs branches concurrently. Real async — no GIL. Best for multi-source research, batch classification.</p>
      </div>
      <div class="outcome">
        <div style="font-size:2rem;margin-bottom:12px;">&#x1f504;</div>
        <h3>Loop &amp; Critic</h3>
        <p><code style="background:var(--surface-code);padding:2px 6px;border-radius:3px;font-size:0.85em">EvalNode</code> scores output, retries with feedback if quality is low. Best for code review, content generation, anything quality-critical.</p>
      </div>
      <div class="outcome">
        <div style="font-size:2rem;margin-bottom:12px;">&#x1f3af;</div>
        <h3>Coordinator</h3>
        <p><code style="background:var(--surface-code);padding:2px 6px;border-radius:3px;font-size:0.85em">CoordinatorNode</code> discovers agents, scores on capability/cost/latency/reasoning modes, and routes dynamically. Async LLM tiebreaker when scores are close. Full scoring in the event log.</p>
      </div>
      <div class="outcome">
        <div style="font-size:2rem;margin-bottom:12px;">&#x1f527;</div>
        <h3>Agent-as-Tool</h3>
        <p><code style="background:var(--surface-code);padding:2px 6px;border-radius:3px;font-size:0.85em">agent_tool()</code> wraps any agent as a callable tool. Sync for quick tasks, real-time incremental streaming with budget guard and idle timeout, conversational for multi-turn refinement.</p>
      </div>
    </div>
  </div>
</section>

<!-- ── JVM Ecosystem Strip ── -->
<section style="background: var(--bg-alt);">
  <div class="container">
    <div class="jvm-strip">
      <div class="jvm-strip-header">
        <span class="section-label">JVM &amp; Spring</span>
        <h2 class="section-title">Java first, not Java later.</h2>
        <p class="jvm-strip-intro">
          On Maven Central today. Agent runtime, memory layer, Spring Boot starter, and LangChain4j integration &mdash;
          all under the <code>dev.jamjet</code> group. Drop-in dependencies, zero custom repositories.
        </p>
      </div>

      <div class="jvm-strip-grid">
        <div class="jvm-snippet">
          <div class="jvm-snippet-label">Agent runtime + memory</div>
          <pre class="jvm-code"><code>&lt;dependency&gt;
  &lt;groupId&gt;dev.jamjet&lt;/groupId&gt;
  &lt;artifactId&gt;jamjet-sdk&lt;/artifactId&gt;
  &lt;version&gt;0.4.3&lt;/version&gt;
&lt;/dependency&gt;</code></pre>
        </div>
        <div class="jvm-features">
          <div class="jvm-feature">
            <strong>Engram memory layer</strong>
            <p>Fact extraction, temporal graph, hybrid retrieval, consolidation. <a href="/blog/state-of-memory-java-ai-agents/">Read the landscape &rarr;</a></p>
          </div>
          <div class="jvm-feature">
            <strong>Spring Boot starters</strong>
            <p><code>jamjet-spring-boot-starter</code> for durability. <code>engram-spring-boot-starter</code> for memory. Auto-configuration, properties binding, Actuator health.</p>
          </div>
          <div class="jvm-feature">
            <strong>LangChain4j integration</strong>
            <p>Use JamJet durability with your existing LangChain4j agents.</p>
          </div>
          <div class="jvm-feature">
            <strong>Virtual threads, records, fluent builders</strong>
            <p>Idiomatic Java 21+ APIs. Not a thin port of a Python SDK.</p>
          </div>
        </div>
      </div>

      <div class="jvm-strip-cta">
        <a href="https://central.sonatype.com/artifact/dev.jamjet/jamjet-sdk" target="_blank" rel="noopener" class="btn-secondary">View on Maven Central</a>
        <a href="https://docs.jamjet.dev/en/docs/java-quickstart" class="btn-secondary">Java quickstart &rarr;</a>
      </div>
    </div>
  </div>
</section>

<!-- ── What's New ── -->
<section>
  <div class="container">
    <span class="section-label">Shipping regularly</span>
    <h2 class="section-title">What's new</h2>
    <div class="whats-new-list">
      <div class="whats-new-item">
        <span class="whats-new-date">Apr 8</span>
        <div class="whats-new-body">
          <strong>Engram Spring Boot Starter on Maven Central</strong>
          <p>
            <code>engram-spring-boot-starter:0.1.1</code> ships auto-configuration, property
            binding via <code>engram.*</code>, and an optional Actuator health indicator.
            Add one dependency, inject <code>EngramClient</code>, done.
          </p>
          <a href="https://central.sonatype.com/artifact/dev.jamjet/engram-spring-boot-starter" target="_blank" rel="noopener">View on Maven Central &rarr;</a>
        </div>
      </div>
      <div class="whats-new-item">
        <span class="whats-new-date">Apr 7</span>
        <div class="whats-new-body">
          <strong>Engram v0.3.1 &mdash; memory layer for AI agents</strong>
          <p>
            Fact extraction, conflict detection, hybrid retrieval (vector + FTS5 keyword + graph),
            token-budgeted context assembly, and a 5-operation consolidation engine.
            Published as <code>jamjet-engram</code> on crates.io and included in
            <code>dev.jamjet:jamjet-sdk</code> on Maven Central.
          </p>
          <a href="/blog/engram-agent-memory/">Read the announcement &rarr;</a>
        </div>
      </div>
      <div class="whats-new-item">
        <span class="whats-new-date">Apr 7</span>
        <div class="whats-new-body">
          <strong>The State of Memory in Java AI Agents</strong>
          <p>
            A landscape survey of every option Java developers have for adding persistent memory
            to AI agents &mdash; LangChain4j, Spring AI, Koog, Embabel, Google ADK, and the rest.
            Why every option stops at chat history.
          </p>
          <a href="/blog/state-of-memory-java-ai-agents/">Read the landscape &rarr;</a>
        </div>
      </div>
      <div class="whats-new-item">
        <span class="whats-new-date">Mar 29</span>
        <div class="whats-new-body">
          <strong>JamJet Spring Boot Starter on Maven Central</strong>
          <p>
            One dependency gives your Spring AI app crash recovery, audit trails, replay testing,
            and human-in-the-loop gates.
          </p>
          <a href="/blog/jamjet-spring-boot-starter/">Read more &rarr;</a>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ── Enterprise Trust Strip ── -->
<section>
  <div class="container">
    <div class="trust-strip">
      <h3>Enterprise-grade when you need it</h3>
      <div class="trust-items">
        <span>Tenant isolation</span>
        <span class="trust-dot"></span>
        <span>PII redaction</span>
        <span class="trust-dot"></span>
        <span>OAuth 2.0 delegation</span>
        <span class="trust-dot"></span>
        <span>mTLS federation</span>
        <span class="trust-dot"></span>
        <span>Retention policies</span>
      </div>
      <p class="trust-note">All enforced at the Rust runtime layer, not by convention.</p>
      <a href="/security" class="trust-link">Security &amp; enterprise docs &rarr;</a>
    </div>
  </div>
</section>

<!-- ── Final CTA ── -->
<section style="background: var(--bg-alt);">
  <div class="container" style="text-align: center; max-width: 560px;">
    <h2>Start building in 60 seconds.</h2>
    <div class="install-cmd" style="margin: 24px auto 32px; max-width: 400px;">
      <span class="prompt">$</span>
      <span class="cmd">jamjet init my-agent</span>
      <button class="copy-btn" id="copy-final">copy</button>
    </div>
    <div style="display: flex; justify-content: center; gap: 12px; flex-wrap: wrap;">
      <a href="https://docs.jamjet.dev/en/docs/quickstart" class="btn-primary">Read the quickstart</a>
      <a href="https://github.com/jamjet-labs/jamjet" class="btn-secondary" target="_blank" rel="noopener">View on GitHub</a>
    </div>
  </div>
</section>

</BaseLayout>

<style>
  /* ── Hero ── */
  .hero {
    min-height: calc(100vh - 64px);
    display: flex; align-items: center; justify-content: center;
    padding: 80px 24px;
    text-align: center;
  }
  .hero-inner { max-width: 720px; }
  .hero-sub {
    font-size: clamp(1rem, 2vw, 1.15rem);
    color: var(--text-secondary);
    margin: 28px 0 48px;
    line-height: 1.8;
  }
  .hero-actions {
    display: flex; justify-content: center; gap: 12px; flex-wrap: wrap;
    margin-bottom: 32px;
  }

  /* ── Section shared ── */
  .section-title {
    margin: 8px 0 48px;
  }
  .section-intro {
    color: var(--text-secondary);
    font-size: 1rem;
    line-height: 1.7;
    margin: 12px 0 40px;
    max-width: 560px;
  }

  /* ── Proof section ── */
  .proof-anim {
    margin-bottom: 48px;
  }
  .proof-grid {
    display: grid;
    grid-template-columns: 1.2fr 1fr;
    gap: 48px;
    align-items: center;
    margin-top: 32px;
  }
  .proof-callouts {
    display: flex;
    flex-direction: column;
    gap: 24px;
  }
  .proof-step {
    display: flex;
    gap: 16px;
    align-items: flex-start;
  }
  .proof-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    min-width: 28px;
    height: 28px;
    border-radius: 50%;
    background: var(--bg-alt);
    border: 1px solid var(--border);
    font-size: 0.75rem;
    font-weight: 600;
    color: var(--text-muted);
    font-family: var(--font-sans);
  }
  .proof-step strong {
    display: block;
    font-size: 0.92rem;
    margin-bottom: 4px;
  }
  .proof-step p {
    color: var(--text-secondary);
    font-size: 0.85rem;
    line-height: 1.5;
    margin: 0;
  }

  /* ── Outcomes ── */
  .outcomes {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 32px 64px;
  }
  .outcome {
    padding: 24px 0;
    border-top: 1px solid var(--border);
  }
  .outcome-icon {
    width: 28px;
    height: 28px;
    color: var(--text-muted);
    margin-bottom: 14px;
    display: block;
  }
  .outcome h3 {
    font-size: 0.95rem;
    font-weight: 600;
    margin-bottom: 8px;
    color: var(--text);
  }
  .outcome p {
    color: var(--text-secondary);
    font-size: 0.88rem;
    line-height: 1.7;
  }

  /* ── Code tabs ── */
  .code-tabs {
    display: flex;
    gap: 0;
  }
  .code-tab {
    background: none;
    border: none;
    color: var(--text-muted);
    font-size: 0.82rem;
    font-weight: 500;
    font-family: var(--font-sans);
    padding: 6px 14px;
    cursor: pointer;
    border-bottom: 2px solid transparent;
    transition: color 0.15s, border-color 0.15s;
  }
  .code-tab:hover {
    color: var(--text-secondary);
  }
  .code-tab.active {
    color: var(--text);
    border-bottom-color: var(--text);
  }
  .code-tab-label {
    font-size: 0.82rem;
    color: var(--text-muted);
  }

  /* ── Code section ── */
  .code-section {
    display: grid;
    grid-template-columns: 1fr 1.4fr;
    gap: 64px;
    align-items: center;
  }
  .code-text h2 { margin-bottom: 8px; }

  /* ── Migration ── */
  .migration-strip {
    display: grid;
    grid-template-columns: 1fr auto;
    gap: 64px;
    align-items: start;
  }
  .migration-points {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 24px 48px;
    margin: 32px 0;
  }
  .migration-point strong {
    display: block;
    font-size: 0.92rem;
    margin-bottom: 6px;
  }
  .migration-point p {
    color: var(--text-secondary);
    font-size: 0.85rem;
    line-height: 1.6;
    margin: 0;
  }
  .migration-actions {
    display: flex; gap: 12px; flex-wrap: wrap;
  }
  .migration-also {
    padding: 24px;
    border-left: 1px solid var(--border);
    min-width: 180px;
  }
  .migration-also-label {
    font-size: 0.75rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--text-muted);
    margin-bottom: 16px;
    font-family: var(--font-sans);
  }
  .migration-also a {
    display: block;
    color: var(--text-secondary);
    font-size: 0.88rem;
    text-decoration: none;
    margin-bottom: 12px;
  }
  .migration-also a:hover {
    color: var(--text);
  }
  .migration-compare {
    margin-bottom: 20px;
  }
  .compare-row {
    padding: 8px 0;
    border-bottom: 1px solid var(--border);
  }
  .compare-name {
    display: block;
    font-size: 0.82rem;
    font-weight: 600;
    color: var(--text);
    font-family: var(--font-sans);
  }
  .compare-desc {
    display: block;
    font-size: 0.78rem;
    color: var(--text-muted);
    margin-top: 2px;
  }
  .compare-highlight {
    border-bottom: 2px solid var(--text);
  }
  .compare-highlight .compare-name {
    color: var(--text);
  }
  .compare-highlight .compare-desc {
    color: var(--text-secondary);
  }

  /* ── Personas ── */
  .personas {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 48px;
  }
  .persona {
    padding: 32px 0;
    border-top: 1px solid var(--border);
  }
  .persona-icon {
    width: 32px;
    height: 32px;
    color: var(--text-muted);
    margin-bottom: 16px;
    display: block;
  }
  .persona-label {
    display: block;
    font-size: 0.68rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: var(--text-muted);
    margin-bottom: 16px;
    font-family: var(--font-sans);
  }
  .persona h3 {
    font-size: 1.05rem;
    font-weight: 600;
    margin-bottom: 12px;
    color: var(--text);
  }
  .persona p {
    color: var(--text-secondary);
    font-size: 0.88rem;
    line-height: 1.7;
    margin-bottom: 16px;
  }
  .persona a {
    color: var(--text);
    font-size: 0.85rem;
    font-weight: 500;
    text-decoration: none;
    font-family: var(--font-sans);
  }
  .persona a:hover { text-decoration: underline; }

  /* ── Research ── */
  .research-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 32px 48px;
  }
  .research-card {
    padding: 24px 0;
    border-top: 1px solid var(--border);
  }
  .research-card-label {
    display: block;
    font-size: 0.68rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: var(--text-muted);
    margin-bottom: 12px;
    font-family: var(--font-sans);
  }
  .research-card p {
    color: var(--text-secondary);
    font-size: 0.9rem;
    line-height: 1.7;
  }

  /* ── Trust strip ── */
  .trust-strip {
    text-align: center;
    padding: 16px 0;
  }
  .trust-strip h3 {
    font-size: 1.05rem;
    font-weight: 600;
    margin-bottom: 20px;
  }
  .trust-items {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 16px;
    flex-wrap: wrap;
    font-size: 0.88rem;
    color: var(--text-secondary);
    font-family: var(--font-sans);
  }
  .trust-dot {
    width: 3px;
    height: 3px;
    border-radius: 50%;
    background: var(--text-muted);
  }
  .trust-note {
    color: var(--text-muted);
    font-size: 0.82rem;
    margin-top: 16px;
    font-family: var(--font-sans);
  }
  .trust-link {
    display: inline-block;
    margin-top: 12px;
    color: var(--text);
    font-size: 0.85rem;
    font-weight: 500;
    text-decoration: none;
    font-family: var(--font-sans);
  }
  .trust-link:hover { text-decoration: underline; }

  /* ── Why This Matters ── */
  .why-matters {
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
  }
  .why-matters-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 32px;
  }
  .why-matters-item strong {
    display: block;
    font-size: 0.92rem;
    margin-bottom: 8px;
    color: var(--text);
  }
  .why-matters-item p {
    color: var(--text-secondary);
    font-size: 0.85rem;
    line-height: 1.6;
    margin: 0;
  }

  /* ── Responsive ── */
  @media (max-width: 768px) {
    .why-matters-grid { grid-template-columns: 1fr 1fr; gap: 24px; }
    .proof-grid { grid-template-columns: 1fr; gap: 32px; }
    .outcomes { grid-template-columns: 1fr; gap: 0; }
    .code-section { grid-template-columns: 1fr; gap: 32px; }
    .migration-strip { grid-template-columns: 1fr; gap: 32px; }
    .migration-also { border-left: none; border-top: 1px solid var(--border); padding: 24px 0 0; min-width: auto; }
    .migration-points { grid-template-columns: 1fr; }
    .personas { grid-template-columns: 1fr; gap: 0; }
    .research-grid { grid-template-columns: 1fr; gap: 0; }
    .trust-items { gap: 8px; }
    .trust-dot { display: none; }
  }
  @media (min-width: 769px) and (max-width: 1024px) {
    .outcomes { grid-template-columns: repeat(2, 1fr); }
    .personas { grid-template-columns: 1fr; gap: 0; }
    .research-grid { grid-template-columns: repeat(2, 1fr); }
    .migration-points { grid-template-columns: 1fr; }
  }

  /* Engram announcement strip */
  .announcement-strip {
    padding: 16px 0 0;
    background: var(--bg);
  }
  .announcement {
    display: flex;
    align-items: center;
    gap: 16px;
    padding: 14px 20px;
    background: var(--bg-alt);
    border: 1px solid var(--border);
    border-radius: 8px;
    text-decoration: none;
    color: var(--text);
    transition: border-color 0.15s ease, transform 0.15s ease;
  }
  .announcement:hover {
    border-color: var(--accent);
    transform: translateY(-1px);
  }
  .announcement-badge {
    flex-shrink: 0;
    font-size: 11px;
    font-weight: 700;
    letter-spacing: 0.08em;
    padding: 4px 10px;
    background: var(--accent);
    color: var(--bg);
    border-radius: 4px;
  }
  .announcement-text {
    flex: 1;
    font-size: 15px;
    line-height: 1.5;
    color: var(--text-secondary);
  }
  .announcement-text strong {
    color: var(--text);
  }
  .announcement-arrow {
    flex-shrink: 0;
    font-size: 18px;
    color: var(--accent);
  }
  @media (max-width: 640px) {
    .announcement { flex-wrap: wrap; gap: 10px; }
    .announcement-text { font-size: 14px; flex-basis: 100%; order: 3; }
    .announcement-arrow { margin-left: auto; }
  }

  /* JVM Ecosystem Strip — expanded */
  .jvm-strip {
    max-width: 1000px;
    margin: 0 auto;
  }
  .jvm-strip-header {
    text-align: center;
    margin-bottom: 40px;
  }
  .jvm-strip-intro {
    max-width: 640px;
    margin: 16px auto 0;
    color: var(--text-secondary);
    line-height: 1.6;
  }
  .jvm-strip-intro code {
    background: var(--bg);
    padding: 2px 6px;
    border-radius: 3px;
    font-size: 0.9em;
  }
  .jvm-strip-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 32px;
    align-items: start;
  }
  .jvm-snippet-label {
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    color: var(--text-secondary);
    margin-bottom: 10px;
  }
  .jvm-code {
    background: var(--bg);
    border: 1px solid var(--border);
    border-radius: 6px;
    padding: 18px;
    margin: 0;
    overflow-x: auto;
    font-size: 13px;
    line-height: 1.6;
  }
  .jvm-code code {
    font-family: 'IBM Plex Mono', 'Consolas', monospace;
    color: var(--text);
  }
  .jvm-features {
    display: flex;
    flex-direction: column;
    gap: 18px;
  }
  .jvm-feature strong {
    display: block;
    color: var(--text);
    margin-bottom: 4px;
  }
  .jvm-feature p {
    font-size: 14px;
    color: var(--text-secondary);
    line-height: 1.5;
    margin: 0;
  }
  .jvm-feature a {
    color: var(--accent);
    text-decoration: none;
  }
  .jvm-feature a:hover {
    text-decoration: underline;
  }
  .jvm-strip-cta {
    display: flex;
    justify-content: center;
    gap: 12px;
    margin-top: 32px;
    flex-wrap: wrap;
  }
  @media (max-width: 768px) {
    .jvm-strip-grid { grid-template-columns: 1fr; gap: 24px; }
  }

  /* What's New */
  .whats-new-list {
    display: flex;
    flex-direction: column;
    gap: 24px;
    max-width: 760px;
    margin: 32px auto 0;
  }
  .whats-new-item {
    display: grid;
    grid-template-columns: 80px 1fr;
    gap: 24px;
    padding-bottom: 24px;
    border-bottom: 1px solid var(--border);
  }
  .whats-new-item:last-child {
    border-bottom: none;
  }
  .whats-new-date {
    font-size: 13px;
    font-weight: 600;
    color: var(--text-secondary);
    padding-top: 2px;
  }
  .whats-new-body strong {
    display: block;
    color: var(--text);
    margin-bottom: 6px;
  }
  .whats-new-body p {
    color: var(--text-secondary);
    line-height: 1.6;
    margin: 0 0 8px 0;
    font-size: 15px;
  }
  .whats-new-body code {
    background: var(--bg-alt);
    padding: 1px 6px;
    border-radius: 3px;
    font-size: 0.88em;
    font-family: 'IBM Plex Mono', 'Consolas', monospace;
  }
  .whats-new-body a {
    color: var(--accent);
    text-decoration: none;
    font-size: 14px;
  }
  .whats-new-body a:hover {
    text-decoration: underline;
  }
  @media (max-width: 640px) {
    .whats-new-item { grid-template-columns: 1fr; gap: 8px; }
  }

  /* ── Pick Your Path ── */
  .pick-path {
    padding: 5rem 0;
  }
  .path-flow {
    max-width: 820px;
    margin: 2.5rem auto 0;
  }
  .path-question {
    text-align: center;
    font-family: var(--font-body);
    font-size: 1.15rem;
    font-weight: 600;
    color: var(--text);
    padding: 1rem 1.5rem;
    border: 2px solid var(--border-strong);
    border-radius: 12px;
    background: var(--bg);
    max-width: 420px;
    margin: 0 auto 2rem;
  }
  .path-branches {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem;
  }
  .path-branch {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
  }
  .path-arrow {
    font-family: var(--font-sans);
    font-size: 0.78rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    padding: 4px 16px;
    border-radius: 20px;
  }
  .path-yes .path-arrow {
    background: rgba(139, 115, 85, 0.12);
    color: var(--accent);
  }
  .path-no .path-arrow {
    background: rgba(26, 26, 24, 0.06);
    color: var(--text-muted);
  }
  .path-card {
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 1.25rem;
    text-align: left;
    transition: border-color 0.15s;
  }
  .path-card:hover {
    border-color: var(--accent);
  }
  .path-card-primary {
    border-color: var(--accent);
    background: rgba(139, 115, 85, 0.04);
  }
  .path-card h3 {
    font-family: var(--font-body);
    font-size: 1rem;
    font-weight: 600;
    margin-bottom: 0.5rem;
  }
  .path-card p {
    font-size: 0.85rem;
    color: var(--text-secondary);
    line-height: 1.6;
    margin-bottom: 0.75rem;
  }
  .path-card code {
    font-family: var(--font-mono);
    font-size: 0.78rem;
    color: var(--text-muted);
  }
  .path-import {
    display: block;
    padding: 6px 10px;
    background: var(--bg-code, rgba(26,26,24,0.04));
    border-radius: 6px;
  }
  .path-fork {
    width: 100%;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }
  .path-subq {
    text-align: center;
    font-family: var(--font-sans);
    font-size: 0.82rem;
    font-weight: 600;
    color: var(--text-muted);
    text-transform: uppercase;
    letter-spacing: 0.08em;
  }
  .path-sub-branches {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }
  @media (max-width: 768px) {
    .path-branches {
      grid-template-columns: 1fr;
    }
  }

  /* ── Capabilities Strip ── */
  .capabilities-strip {
    margin-top: 3rem;
    padding-top: 2.5rem;
    border-top: 1px solid var(--border);
  }
  .capabilities-label {
    display: block;
    text-align: center;
    font-family: var(--font-sans);
    font-size: 0.72rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--text-muted);
    margin-bottom: 1.25rem;
  }
  .capabilities-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1rem;
    max-width: 820px;
    margin: 0 auto;
  }
  .cap-item {
    display: flex;
    flex-direction: column;
    gap: 6px;
    padding: 1rem;
    border: 1px solid var(--border);
    border-radius: 10px;
    text-decoration: none;
    color: inherit;
    transition: border-color 0.15s;
  }
  .cap-item:hover {
    border-color: var(--accent);
  }
  .cap-icon {
    color: var(--text-muted);
    margin-bottom: 2px;
  }
  .cap-name {
    font-family: var(--font-body);
    font-size: 0.92rem;
    font-weight: 600;
  }
  .cap-desc {
    font-family: var(--font-sans);
    font-size: 0.78rem;
    color: var(--text-muted);
    line-height: 1.5;
  }
  @media (max-width: 768px) {
    .capabilities-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  @media (max-width: 480px) {
    .capabilities-grid {
      grid-template-columns: 1fr;
    }
  }
</style>

<script>
  document.getElementById('copy-install')?.addEventListener('click', function() {
    navigator.clipboard.writeText('pip install jamjet').then(() => {
      this.textContent = 'copied!';
      setTimeout(() => { this.textContent = 'copy'; }, 1500);
    });
  });
  document.getElementById('copy-final')?.addEventListener('click', function() {
    navigator.clipboard.writeText('jamjet init my-agent').then(() => {
      this.textContent = 'copied!';
      setTimeout(() => { this.textContent = 'copy'; }, 1500);
    });
  });

  // Code tab switching
  const tabLabels = { python: 'durable workflow', java: 'agent builder' };
  document.querySelectorAll('.code-tab').forEach(function(tab) {
    tab.addEventListener('click', function() {
      const lang = this.getAttribute('data-tab');
      document.querySelectorAll('.code-tab').forEach(function(t) { t.classList.remove('active'); });
      this.classList.add('active');
      document.getElementById('code-python').style.display = lang === 'python' ? '' : 'none';
      document.getElementById('code-java').style.display = lang === 'java' ? '' : 'none';
      var label = document.getElementById('code-tab-label');
      if (label) label.textContent = tabLabels[lang] || '';
    });
  });
</script>
